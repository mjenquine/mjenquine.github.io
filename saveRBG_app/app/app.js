$(() => {
//=====================================
  const $openBtn = $('#open-modal-btn');
  const $modal = $('#modal');

  const openModal = () => {
    $modal.css('display', 'block');
  }

  $openBtn.on('click', openModal)
































//===================================
})
